"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

import cStringIO as StringIO
import struct

import object_t

class object_list_t(object):
    __slots__ = ["utime", "num_objects", "objects"]

    def __init__(self):
        self.utime = 0
        self.num_objects = 0
        self.objects = []

    def encode(self):
        buf = StringIO.StringIO()
        buf.write(object_list_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.utime, self.num_objects))
        for i0 in range(self.num_objects):
            self.objects[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = StringIO.StringIO(data)
        if buf.read(8) != object_list_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return object_list_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = object_list_t()
        self.utime, self.num_objects = struct.unpack(">qi", buf.read(12))
        self.objects = []
        for i0 in range(self.num_objects):
            self.objects.append(object_t.object_t._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if object_list_t in parents: return 0
        newparents = parents + [object_list_t]
        tmphash = (0x92af6330dbe6ff69+ object_t.object_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff 
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None
    
    def _get_packed_fingerprint():
        if object_list_t._packed_fingerprint is None:
            object_list_t._packed_fingerprint = struct.pack(">Q", object_list_t._get_hash_recursive([]))
        return object_list_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

